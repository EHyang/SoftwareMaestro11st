const reload = require('./reload');
const login = require('./login');
const scan = require('./scan');
const confirm = require('./confirm');
const state = require('./state');
const stateAll = require('./stateAll')

module.exports = {
    reload,
    login,
    scan,
    confirm,
    state,
    stateAll,
}